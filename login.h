#ifndef LOGIN_H // checks if already been defined
#define LOGIN_H //if not already defined, this line defines it

#define MAX_USERNAME 50
#define MAX_PASSWORD 50

// Function prototypes
int registerUser();
int loginUser();

#endif // marks the end of ifndef